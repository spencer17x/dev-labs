# Twitter Bot Configuration Example
# 复制此文件为 .env 并填入真实的值

# ===========================================
# Twitter 认证配置 (必需)
# ===========================================
# 获取方式：
# 1. 登录 Twitter 网页版
# 2. 打开开发者工具 (F12)
# 3. 在 Application -> Storage -> Cookies 中找到以下值

# Twitter 的 CSRF Token
ct0=你的_ct0_值_通常是32位字符

# Twitter 的认证令牌
auth_token=你的_auth_token_值_通常以数字字母组合开头

# 检查更新间隔 (秒)
# 建议值: 30-60 秒，避免频率过高被限制
interval=30

# ===========================================
# Telegram Bot 配置 (必需)
# ===========================================
# 获取方式：
# 1. 联系 @BotFather 创建机器人
# 2. 获取 Bot Token

# Telegram Bot Token
tg_bot_token=123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghi

# 注意：不再需要 tg_chat_ids 配置
# 机器人会自动维护所在群组列表，推文将转发到所有机器人所在的群组
# 只需将机器人添加到目标群组即可

# ===========================================
# 可选配置
# ===========================================
# 日志级别 (可选)
LOG_LEVEL=info

# 数据库文件路径 (可选，默认为 db.json)
DB_PATH=./db.json

# ===========================================
# 部署相关配置 (可选)
# ===========================================
# Node.js 环境
NODE_ENV=production

# PM2 配置 (如果使用 PM2)
PM2_APP_NAME=twitter-bot
